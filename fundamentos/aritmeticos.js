const [a, b, c, d] = [3, 5, 1, 15];

const sum = a + b + c + d;
const subtraction = d - b;
const multiplication = a * b;
const division = d / a;
const modulo = a % 2;

console.log(sum, subtraction, division, modulo, multiplication);